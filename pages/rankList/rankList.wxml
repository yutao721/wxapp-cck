<page-meta page-style="{{ show ? 'overflow: hidden;' : '' }}" />
<view class="wrap">
  <view class="header">
    <view class="header-tips"></view>
    <view class="header-rule" bindtap="handleOpen">规则说明</view>
  </view>
  <view class="rank-wrap">
    <view class="rank-wrap-header"></view>
    <view class="lists">
      <view class="list-hd">
        <view class="rank">排名</view>
        <view class="user {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">用户</view>
        <view class="total" wx:if="{{activeTab == 1}}">答题总数</view>
        <view class="list-name" wx:if="{{activeTab == 3}}">考试名称</view>
        <view class="right-num" wx:if="{{activeTab == 1}}">对题数</view>
        <view class="right-num" wx:if="{{activeTab == 2}}">闯关数</view>
        <view class="right-num" wx:if="{{activeTab == 3}}">分数</view>
        <view class="right-num" wx:if="{{activeTab == 4}}">积分</view>
      </view>
      <view class="list-bd">
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
        <view class="list">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
      </view>

      <!--自己的排名-->
      <view class="list-bd">
        <view class="list fixed">
          <view class="rank rank-bd">1</view>
          <view class="user user-bd {{(activeTab == 2 || activeTab == 4) ? 'w400': activeTab == 3 ?  'w238' : 'w256'}}">
            <view class="avatar"></view>
            <view class="name">张小花就是张小花就张小花就是张小花就张小花就是张小花就</view>
          </view>
          <view class="total total-bd" wx:if="{{activeTab == 1}}">489</view>
          <view class="list-name list-name-bd" wx:if="{{activeTab == 3}}">考试名称考试名称考试名称考试名称</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 1}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 2}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 3}}">324</view>
          <view class="right-num right-num-bd" wx:if="{{activeTab == 4}}">324</view>
        </view>
      </view>
    </view>
  </view>

  <van-popup show="{{show}}"
             bind:close="onClose"
             round>
    <view class="rule-wrap">
      <view class="rule-close" bindtap="onClose"></view>
      <view class="rule-title">规则说明</view>
      <view class="rule-content">
        <view class="rule-list">
          <text class="rule-list-icon"></text>
          <text class="rule-list-text">用户答题正确数的排名，答题正确数越高，排名越靠前；</text>
        </view>
        <view class="rule-list">
          <text class="rule-list-icon"></text>
          <text class="rule-list-text">统计对象包含答题PK、题目闯关试卷考试和题库答题；</text>
        </view>
        <view class="rule-list">
          <text class="rule-list-icon"></text>
          <text class="rule-list-text">温馨提示：中途退出、手机故障、网络不佳等均会影响统计结果。</text>
        </view>
      </view>
      <view class="rule-btn" bindtap="onClose">我知道了</view>
    </view>
  </van-popup>
</view>



